import { h, FunctionalComponent } from 'preact';
import './statistics.scss';

export interface statisticsProps {

}

const statistics: FunctionalComponent<statisticsProps> = ({  }) => {
	return (
		<div>
			Hello statistics
		</div>
	)
}

export default statistics;
