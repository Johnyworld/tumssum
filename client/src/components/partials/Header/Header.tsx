import { h, FunctionalComponent } from 'preact';
import './Header.scss';

interface Props {

}

const Header: FunctionalComponent<Props> = ({  }) => {
	return (
		<header class='header'>
			Hello Header
		</header>
	)
}

export default Header;
